# include <stdio.h>
# include <string.h>
# define MAX	50

char * get_token( FILE *fp ) {
	char word[MAX];
	while ( getc ( fp ) != EOF ) {
		if ( c != ':' && c != '\' && c != ' ' && c != '\n' && c != '\t' )
			word[i++] = c;
		if ( c == ':' && prev_c != '\' )
			return ":" ;
		if ( c == '\' )
			continue;

	if ( c == '\n' && prev_c == '\' )
		
		if ( c == ' ' ) contunie;

		
